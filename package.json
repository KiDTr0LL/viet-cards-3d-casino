{
  "name": "viet-cards-3d-casino",
  "version": "1.0.0",
  "private": true,
  "description": "Premium Vietnamese Card Games Casino - 3D Rebuild",
  "workspaces": [
    "apps/*",
    "packages/*",
    "server/*",
    "shared/*"
  ],
  "scripts": {
    "dev:rn": "npm run start -w @viet-cards/mobile-rn",
    "dev:server": "npm run dev -w @viet-cards/server",
    "build:all": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
